# 📦 وثيقة تسليم المشروع — "ذكاء ربحي" (النسخة الاحترافية)

**تاريخ التسليم:** 2026-08-29 (نهاية الوردي الليلي)
**الحالة:** ✅ الموقع مكتمل وجاهز للرفع

---

## 1) ما الذي بُني هذه الليلة

| القسم | الإنجاز |
|---|---|
| **التصميم** | نظام تصميم احترافي كامل: هوية بصرية، خط IBM Plex Sans Arabic، ألوان موحدة، متجاوب مع الجوال |
| **المحتوى** | **18 مقالًا حقيقيًا** (13 سابقًا + 5 جديدة) — كلها مفحوصة |
| **الصفحات** | الرئيسية + صفحة مقال (بشريط تقدم قراءة + مشاركة واتساب/تويتر + "اقرأ أيضًا") + عن الموقع + تواصل + سياسة الخصوصية + صفحة 404 |
| **SEO** | وسوم Meta/OG/Twitter كاملة، بيانات منظمة (JSON-LD)، sitemap مع lastmod، canonical، robots.txt |
| **الهوية البصرية** | شعار SVG (صاعقة) + صورة مشاركة اجتماعية مولدة |
| **محرك التوليد** | جاهز: يكتب مقالًا يوميًا عبر Groq (المحرر) — ملف "الفرن" في `.github/workflows/` |
| **قائمة المواضيع** | 17 موضوعًا بانتظار الفرن التلقائي + 13 مقالًا = شهر كامل من المحتوى |

## 2) كيف تستلم الموقع غدًا (خياران)

### الخيار أ — الأستر: رفع فوري بدون حساب جديد (2 دقائق)
1. حمّل ملف `site-final.zip` من مساحة العمل
2. افتح: `https://app.netlify.com/drop`
3. اسحب الحزمة وأفلتها
4. خلال دقيقة: موقعك حيّ على رابط دائم (`xxx.netlify.app`)

**ثم أرسل لي الرابط الجديد** وسأضبط به الإعدادات (السيت ماب وروابط المشاركة) وأعيد تجهيز الحزمة المحسنة.

> 💡 إن كان عندك حساب Netlify من قبل: من صفحة المشروع → "Site configuration" → "Change site name" يمكنك اختيار اسم نظيف مثل `zika-rabahi` (بدل الاسم العشوائي).

### الخيار ب — الأتمتة الكاملة (10 دقائق غدًا، معي خطوة بخطوة)
1. حساب GitHub جديد بـ**البريد الجديد** الذي تفضله
2. مفتاح Groq جديد (5 دقائق — نفس الخطوات السابقة)
3. رمز GitHub مؤقت: **مهم — ضع علامة على الصلاحيتين: `repo` و `workflow`**
4. أرسلهما لي → أنفذ كل شيء بنفسي (المستودع + الخزينة + الفرن)
5. النتيجة: مقال جديد **كل صباح الساعة 11** على رابط دائم باسم المنظمة — بدون أي تدخل

## 3) تنظيف الحساب القديم (إلغاء ما بدأنا به أمس)

لإغلاق ما بدأنا به بالبريد القديم — 4 خطوات (كلها من الواجهة):

1. **Netlify:** مشروع `stately-cobbler-ed09aa` → Settings → Danger zone → **Delete site**
2. **GitHub:** حذف المنظمة `zika-rabahi` (إن أُنشئت) — github.com/orgs/zika-rabahi → Settings → Danger zone
3. **GitHub:** حذف المستودع `abo7meed21-beep/ai-content-site` — Settings → Danger zone → Delete
4. **GitHub:** حذف الرمز "Setup" — (ينتهي تلقائيًا 5 سبتمبر إن نسيت)

> ⚠️ لا تحذف حساب GitHub القديم نفسه إن كنت تستخدمه لغير هذا المشروع.

## 4) خطة الدخل (تبدأ بعد أسبوعين من التشغيل)

| القناة | متى | متوقع |
|---|---|---|
| Google AdSense | بعد ~30 مقالًا (أسبوعان) | 0–150$ في أول 3 أشهر |
| روابط العمولة | من الآن (تُضاف في المقالات) | عمولات متكررة |
| منتج رقمي (دليل 10–15$ على Gumroad) | بعد 3 أشهر | يضاعف الدخل |
| نطاق حقيقي (zika-rabahi.com ~10$/سنة) | عند أول دخل | صورة احترافية نهائية |

## 5) بنية الملفات

```
zika-rabahi/
├── public/               ← الموقع النهائي (داخل site-final.zip)
├── content/              ← 18 مقالًا (Markdown)
├── engine/               ← محرك التوليد والنشر (Python)
├── templates/            ← القوالب (رئيسية، مقال، صفحات ثابتة)
├── assets/               ← التصميم + الشعار + صورة المشاركة
├── .github/workflows/    ← ملف "الفرن" اليومي
├── data/processed.json   ← المواضيع المستهلكة (منع التكرار)
├── topics.txt            ← 30 موضوعًا (17 بانتظار الفرن)
├── config.json           ← كل الإعدادات (اسم الموقع، البريد، المحرر)
└── site-final.zip        ← الحزمة الجاهزة للرفع
```

## 6) ملاحظات التشغيل

- **البريد في صفحة التواصل** حاليًا `info@zika-rabahi.com` — عدّله في `config.json` عند امتلاكك بريدًا حقيقيًا
- **الإعلانات:** عند قبول Google AdSense، الصق كود الوحدة في `adsense_slot` بـ `config.json` وستظهر في كل مقال تلقائيًا
- **إضافة مواضيع:** سطر واحد في `topics.txt` = مقال جديد غدًا

---

**صُنع بذكاء، وفُحص بإنسان. — فريق ذكاء ربحي** ⚡
